import { Box, Typography } from "@mui/material";

export const ServiceWidget = () => {
    return (
        <Box>
            <Typography>Состояние:</Typography>
            <Typography>До опроса каналов:</Typography>
            <Typography>До опроса комментариев:</Typography>
        </Box>
    );
};
